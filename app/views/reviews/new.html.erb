<%# http://localhost:3000/reviews/new %>

<h1><strong>Add a Review</strong></h1>

<h3>
<%# below is a function that has one arguement (the array) %>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit %>
  <% end %>
</h3>

<h4><%= link_to "Back to Restaurants List", restaurants_path %></h4>
